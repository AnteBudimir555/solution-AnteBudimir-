{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning",
        "Microsoft.AspNetCore.Diagnostics.ExceptionHandlerMiddleware": "Fatal"
      }
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "Provider": "Sqlite"
  },
  "Upstream": {
    "//": "ResponseTimeoutMs is the budget for the whole call including retries; AttemptTimeoutMs bounds one attempt. Retries fit inside the existing budget rather than extending it.",
    "BaseUrl": "https://dummyjson.com",
    "ConnectTimeoutMs": 3000,
    "ResponseTimeoutMs": 5000,
    "AttemptTimeoutMs": 2000,
    "MaxInMemoryCandidates": 5000,
    "RetryAttempts": 2,
    "RetryBaseDelayMs": 200,
    "CircuitBreakerFailureRatio": 0.5,
    "CircuitBreakerMinimumThroughput": 20,
    "CircuitBreakerSamplingDurationMs": 30000,
    "CircuitBreakerBreakDurationMs": 5000,
    "PooledConnectionLifetimeMinutes": 5
  },
  "Summary": {
    "DescriptionMaxLength": 100
  },
  "Cache": {
    "//": "MaximumSizeBytes is a byte budget, not an entry count — see CacheOptions for why the Caffeine entry count could not carry across.",
    "MaximumSizeBytes": 67108864,
    "MaximumEntryBytes": 1048576,
    "ExpireAfterWriteSeconds": 60,
    "CategoriesExpireAfterWriteSeconds": 3600
  },
  "Jwt": {
    "//": "No default secret is shipped: supply it via configuration/environment. A missing or short secret fails start-up.",
    "Secret": "",
    "ExpirationMinutes": 60,
    "Issuer": "abysalto-middleware"
  },
  "Security": {
    "//": "Disabled by default so production-like configurations never create a known-credential account.",
    "SeedUser": {
      "Enabled": false,
      "Username": "",
      "Password": ""
    }
  },
  "Cors": {
    "//": "Browser origins allowed to read responses. Empty means none, which is correct for a server-to-server deployment. Exact origins only: no trailing slash, no path — start-up fails on either.",
    "AllowedOrigins": []
  },
  "OpenApi": {
    "Title": "Product Middleware API",
    "Description": "Middleware that re-exposes DummyJSON products with a trimmed shape, filtering, name search, JWT auth and caching.",
    "Version": "v1"
  }
}
